# /tests/calib/record_data/CmakeLists.txt

find_package(OpenCV REQUIRED)

set(TEST_RECORD_CALIB_SOURCES
    test_record.cpp
)

add_executable(test_record ${TEST_RECORD_CALIB_SOURCES})

target_link_libraries(test_record
    PRIVATE
        eyetracker::cam
        eyetracker::cfg
        ${OpenCV_LIBS}
)

message(STATUS "✅ test_record_calib will be built")

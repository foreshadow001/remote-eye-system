# /tests/inference/CmakeLists.txt

find_package(OpenCV CONFIG REQUIRED)

set(TEST_GAZE_SOURCES
    test_two_eye.cpp
)

add_executable(test_two_eye ${TEST_GAZE_SOURCES})

target_link_libraries(test_two_eye
    PRIVATE
        eyetracker::cfg
        eyetracker::inference
        eyetracker::glint_detection
        eyetracker::pupil_center
        eyetracker::utils
        ${OpenCV_LIBS}
)
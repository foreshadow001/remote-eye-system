add_executable(test_multi_cam_multi_host
    test_multi_cam_multi_host.cpp
)

target_link_libraries(test_multi_cam_multi_host
    PRIVATE
        eyetracker::cfg
        eyetracker::cam
)

message(STATUS "test_multi_cam_multi_host will be built and linked with eyetracker::cam")

find_package(HDF5 REQUIRED COMPONENTS CXX)

add_executable(test_multi_cam_multi_host_hdf5
    test_multi_cam_multi_host_hdf5.cpp
)

target_link_libraries(test_multi_cam_multi_host_hdf5
    PRIVATE
        eyetracker::cfg
        eyetracker::cam
        ${HDF5_CXX_LIBRARIES}
)

target_include_directories(test_multi_cam_multi_host_hdf5
    PRIVATE
        ${HDF5_INCLUDE_DIRS}
)

message(STATUS "test_multi_cam_multi_host_hdf5 will be built")

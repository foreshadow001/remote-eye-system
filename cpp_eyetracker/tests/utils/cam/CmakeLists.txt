add_executable(test_multi_cam
    test_multi_cam.cpp
)

target_link_libraries(test_multi_cam
    PRIVATE
        eyetracker::cfg
        eyetracker::cam
)

message(STATUS "test_multi_cam will be built and linked with eyetracker::cam")

add_executable(test_multi_cam_multi_host
    test_multi_cam_multi_host.cpp
)

target_link_libraries(test_multi_cam_multi_host
    PRIVATE
        eyetracker::cfg
        eyetracker::cam
)

message(STATUS "test_multi_cam_multi_host will be built and linked with eyetracker::cam")

find_package(OpenCV REQUIRED)
find_package(HDF5 REQUIRED COMPONENTS CXX)

add_executable(test_multi_cam_multi_host_hdf5
    test_multi_cam_multi_host_hdf5.cpp
)

target_link_libraries(test_multi_cam_multi_host_hdf5
    PRIVATE
        eyetracker::cfg
        eyetracker::cam
        ${HDF5_CXX_LIBRARIES}
)

target_include_directories(test_multi_cam_multi_host_hdf5
    PRIVATE
        ${HDF5_INCLUDE_DIRS}
)

message(STATUS "test_multi_cam_multi_host_hdf5 will be built")

add_executable(test_load_hdf5_frame
    test_load_hdf5_frame.cpp
)

target_link_libraries(test_load_hdf5_frame
    PRIVATE
        eyetracker::cfg
        ${OpenCV_LIBS}
        ${HDF5_CXX_LIBRARIES}
)

target_include_directories(test_load_hdf5_frame
    PRIVATE
        ${HDF5_INCLUDE_DIRS}
        ${OpenCV_INCLUDE_DIRS}
)

message(STATUS "test_load_hdf5_frame will be built")

# ---- Multi-process HDF5 dump ----

add_executable(hdf5_multi_process
    hdf5_multi_process.cpp
)

target_link_libraries(hdf5_multi_process
    PRIVATE
        eyetracker::cfg
        eyetracker::cam
        ${HDF5_CXX_LIBRARIES}
)

target_include_directories(hdf5_multi_process
    PRIVATE
        ${HDF5_INCLUDE_DIRS}
)

message(STATUS "hdf5_multi_process will be built")

add_executable(hdf5_multi_process_child
    hdf5_multi_process_child.cpp
)

target_link_libraries(hdf5_multi_process_child
    PRIVATE
        ${HDF5_CXX_LIBRARIES}
)

target_include_directories(hdf5_multi_process_child
    PRIVATE
        ${HDF5_INCLUDE_DIRS}
)

message(STATUS "hdf5_multi_process_child will be built")

# ---- capture_with_piper (Master+Slave cameras + Piper arm control) ----
add_executable(capture_with_piper
    capture_with_piper.cpp
)

target_link_libraries(capture_with_piper
    PRIVATE
        eyetracker::piper
        eyetracker::cfg
        eyetracker::cam
        ${HDF5_CXX_LIBRARIES}
)

target_include_directories(capture_with_piper
    PRIVATE
        ${HDF5_INCLUDE_DIRS}
)

message(STATUS "capture_with_piper will be built")

add_executable(capture_with_LED
    capture_with_LED.cpp
)

target_link_libraries(capture_with_LED
    PRIVATE
        eyetracker::piper
        eyetracker::cfg
        eyetracker::cam
        ${HDF5_CXX_LIBRARIES}
)

target_include_directories(capture_with_LED
    PRIVATE
        ${HDF5_INCLUDE_DIRS}
)

message(STATUS "capture_with_LED will be built")

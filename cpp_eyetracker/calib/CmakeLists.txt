# /calib/CmakeLists.txt

add_subdirectory(set_image)

add_library(calib INTERFACE)
add_library(eyetracker::calib ALIAS calib)

target_include_directories(calib INTERFACE
    $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/include>
    $<INSTALL_INTERFACE:include>
)

find_package(Ceres CONFIG REQUIRED)

target_link_libraries(calib 
    INTERFACE
        eyetracker::utils
        Ceres::ceres
)
